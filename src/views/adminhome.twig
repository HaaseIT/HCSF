<h1>Welcome to the administration area</h1>
<h3>Quick check of required PHP Extensions:</h3>

<p>Filter is: {{ customdata.filter_enabled ? '<span style="color:green">enabled' :  '<span style="color:red">disabled' }}</span></p>

<h3>Quick check of required file/directory permissions:</h3>

<p>Template cache {{ customdata.path_templatecache }} exists:
    {% if customdata.path_templatecache_exists %}
        <span style="color:green">YES</span>, and it is
        {{ customdata.path_templatecache_writable ? '<span style="color:green">' : '<span style="color:red">NOT ' }}writable</span>
    {% else %}
        <span style="color:red">NO!</span>
    {% endif %}
</p>

<p>HTMLPurifier cache {{ customdata.path_purifiercache }} exists:
    {% if customdata.path_purifiercache_exists %}
        <span style="color:green">YES</span>, and it is
        {{ customdata.path_purifiercache_writable ? '<span style="color:green">' : '<span style="color:red">NOT ' }}writable</span>
    {% else %}
        <span style="color:red">NO!</span>
    {% endif %}
</p>

{% if customdata.enable_module_shop %}
<p>Log Directory {{ customdata.path_logs }} exists:
    {% if customdata.path_logs_exists %}
    <span style="color:green">YES</span>, and it is
    {{ customdata.path_logs_writable ? '<span style="color:green">' : '<span style="color:red">NOT ' }}writable</span>.
    {% else %}
        <span style="color:red">NO!</span>
    {% endif %}
</p>
{% endif %}

{% if customdata.check_mod_rewrite %}
    <p>The Apache module mod_rewrite is {{ customdata.mod_rewrite_available ? '<span style="color:green">' : '<span style="color:red">NOT ' }}available</span>.</p>
{% endif %}

<hr>
<form action="{{ requesturi }}" method="POST" class="pure-form">
    <fieldset>
        <legend>String encryption tool for admin passwords (set in config.scrts.yml)</legend>
    <label for="string">String to encrypt:</label><br>
    <input type="text" id="string" name="string" class="pure-u-1-2" value="{{ gFF("string") }}">
        <button type="submit" class="pure-button pure-button-primary pure-u-1-2">Submit</button>
    </fieldset>
</form>

{% if customdata.encrypted_string %}
<p>
    The encrypted form of your submitted string <em>{{ gFF("string") }}</em> is:<br>
    <form class="pure-form"><input type="text" readonly value="{{ customdata.encrypted_string }}" class="pure-u-1-2"></form>
</p>
{% endif %}

{{content}}
